# 📱 TurjiTrade PWA Setup Guide

## מדריך התקנת TurjiTrade כאפליקציה במסך הבית

TurjiTrade היא עכשיו Progressive Web App (PWA) מלאה! אתה יכול להתקין אותה במסך הבית של המכשיר שלך לחוויה משופרת.

---

## ✨ מה זה PWA?

PWA היא אפליקציית ווב מתקדמת שמתנהגת כמו אפליקציה נייטיבית:
- **התקנה למסך הבית** - אייקון משלה במסך הבית
- **עבודה במצב אופליין** - פונקציונליות בסיסית גם בלי אינטרנט
- **מסך מלא** - בלי שורת כתובת של הדפדפן
- **מהירות** - זמני טעינה מהירים יותר
- **התראות** - (בעתיד) קבלת התראות על עדכונים

---

## 📲 איך להתקין באנדרואיד (Chrome/Samsung Internet)

1. **פתח את האפליקציה** בדפדפן
2. **הקש על הסמל** בפינה הימנית העליונה (⋮)
3. **בחר "הוסף למסך הבית"** או "התקן אפליקציה"
4. **אשר את ההתקנה**

האפליקציה תופיע עכשיו במסך הבית שלך עם הלוגו של TurjiTrade!

---

## 🍎 איך להתקין ב-iOS (Safari)

1. **פתח את האפליקציה** ב-Safari
2. **הקש על כפתור השיתוף** (התיבה עם חץ למעלה) בתחתית המסך
3. **גלול למטה** ומצא "הוסף למסך הבית"
4. **ערוך את השם** (אופציונלי) ו**לחץ על הוסף**

---

## 💻 איך להתקין במחשב (Chrome/Edge)

1. **פתח את האפליקציה** בדפדפן
2. **חפש אייקון התקנה** (➕) בשורת הכתובת
3. **לחץ על "התקן"**
4. **אשר את ההתקנה**

האפליקציה תיפתח בחלון נפרד משלה!

---

## 🎨 הלוגו והאייקונים

TurjiTrade כוללת לוגו מותאם אישית:
- **אות T** מעוצבת עם **גרדיאנט ירוק אמרלד**
- **גרף מניות** המסמל מסחר
- **אייקון AI** (ניצוץ) המייצג את הבינה המלאכותית
- **רקע כהה** המתאים לעיצוב האפליקציה

הלוגו זמין בכל הגדלים הנדרשים (72x72 עד 512x512 פיקסלים).

---

## 🔧 קבצי PWA שנוצרו

### קבצים עיקריים:
- **`/index.html`** - דף HTML ראשי עם מטא-טאגים ל-PWA
- **`/public/manifest.json`** - קובץ המניפסט של ה-PWA
- **`/public/sw.js`** - Service Worker למצב אופליין
- **`/public/logo.svg`** - הלוגו הראשי
- **`/public/icon-[SIZE].svg`** - אייקונים בגדלים שונים
- **`/src/main.tsx`** - קובץ הכניסה הראשי עם רישום ל-Service Worker
- **`/src/app/components/InstallPrompt.tsx`** - רכיב שמציג הודעת התקנה

### קבצי עזר:
- **`/generate-icons.js`** - סקריפט ליצירת האייקונים
- **`/public/generate-icons.html`** - כלי לייצור אייקוני PNG (אם נדרש)

---

## 🚀 הרצה והפעלה

### התקנת התלויות והרצה:
```bash
npm install
npm run dev
```

### יצירת האייקונים (אופציונלי):
```bash
node generate-icons.js
```

### בניית הפרודקשן:
```bash
npm run build
npm run preview
```

---

## 📱 תכונות PWA נוספות שניתן להוסיף בעתיד

- ✅ **התקנה למסך הבית** - מוכן!
- ✅ **אייקון ולוגו מותאם אישית** - מוכן!
- ✅ **Service Worker** - מוכן!
- ⏳ **התראות Push** - ניתן להוסיף
- ⏳ **סנכרון ברקע** - ניתן להוסיף
- ⏳ **שיתוף קבצים** - ניתן להוסיף

---

## 🛠️ פתרון בעיות

### האייקון לא מופיע?
- וודא שהקבצים ב-`/public` נמצאים במיקום הנכון
- נקה את ה-cache של הדפדפן
- בדוק ב-DevTools -> Application -> Manifest

### התקנה לא עובדת?
- וודא ש-HTTPS פעיל (נדרש ל-PWA)
- בדוק שה-manifest.json תקין
- וודא שה-Service Worker רשום בהצלחה

### האפליקציה לא עובדת אופליין?
- בדוק את קובץ ה-Service Worker ב-DevTools
- וודא שהקבצים הדרושים במטמון

---

## 📚 מסמכים נוספים

- [PWA Documentation (MDN)](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)
- [Web App Manifest (W3C)](https://www.w3.org/TR/appmanifest/)
- [Service Worker API](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API)

---

**נבנה עם ❤️ על ידי צוות TurjiTrade**
