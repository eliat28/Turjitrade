// ═════════════════════════════════════════════════════════════════
// 🔐 TurjiTrade - הגדרות API מרכזיות
// ═════════════════════════════════════════════════════════════════
// 
// ⚠️ חשוב: קובץ זה מכיל מפתחות API רגישים!
// לשימוש משפחתי/פרטי בלבד.
//
// הגדר את המפתחות שלך כאן פעם אחת, והמערכת תשלח התראות
// לכל המשתמשים מהחשבון המרכזי שלך.
// ═════════════════════════════════════════════════════════════════

export const API_CONFIG = {
  // ────────────────────────────────────────────────────────────────
  // 📧 EmailJS Configuration
  // ────────────────────────────────────────────────────────────────
  // לקבלת מפתחות: https://www.emailjs.com/
  // 1. צור חשבון ב-EmailJS
  // 2. צור Email Service (Gmail/Outlook וכו')
  // 3. צור Email Template
  // 4. העתק את המפתחות לכאן:
  
  emailjs: {
    serviceId: 'service_hilo7f9',        // EmailJS Service ID
    templateId: 'template_mf55m0s',      // EmailJS Template ID
    publicKey: 'FGP5h_sOZYsVznJ54',      // EmailJS Public Key
  },

  // ────────────────────────────────────────────────────────────────
  // 📱 Telegram Bot Configuration
  // ────────────────────────────────────────────────────────────────
  // לקבלת Bot Token:
  // 1. פתח שיחה עם @BotFather בטלגרם
  // 2. שלח /newbot ועקוב אחרי ההוראות
  // 3. העתק את ה-Bot Token לכאן:
  // 
  // ⚠️ חשוב: הבוט שלך (@TurjiTrade_Bot) כבר מוכן!
  // המשתמשים צריכים רק לשלוח /start לבוט כדי לקבל התראות.
  
  telegram: {
    botToken: '8264340445:AAHvwQQAHwfnnDQdGhOxGv9uB2pDEG3cPpU',          // Bot Token של @TurjiTrade_Bot
  },

  // ────────────────────────────────────────────────────────────────
  // 📊 Finnhub API Configuration
  // ────────────────────────────────────────────────────────────────
  // לקבלת API Key: https://finnhub.io/
  // הכנס את המפתח שלך ב-localStorage דרך הממשק
  
  finnhub: {
    // API key נשמר ב-localStorage: 'turjiTrade_finnhub_api_key'
  }
};

// ────────────────────────────────────────────────────────────────
// 🔍 Helper Functions
// ────────────────────────────────────────────────────────────────

export const isEmailConfigured = (): boolean => {
  return (
    API_CONFIG.emailjs.serviceId !== 'YOUR_SERVICE_ID' &&
    API_CONFIG.emailjs.templateId !== 'YOUR_TEMPLATE_ID' &&
    API_CONFIG.emailjs.publicKey !== 'YOUR_PUBLIC_KEY'
  );
};

export const isTelegramConfigured = (): boolean => {
  return API_CONFIG.telegram.botToken !== 'YOUR_BOT_TOKEN';
};

export const getEmailConfig = () => {
  return API_CONFIG.emailjs;
};

export const getTelegramConfig = () => {
  return API_CONFIG.telegram;
};